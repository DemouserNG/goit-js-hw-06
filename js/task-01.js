const numberCategoriesEl = document.querySelectorAll('li.item');
console.log('Number of categories:', numberCategoriesEl.length);

const titleEl = document.querySelector('h2');
const textContentEl = titleEl.textContent;
console.log('Category:', textContentEl);

const navElement = document.querySelectorAll('ul');
console.log('Elements:', navElement.length);